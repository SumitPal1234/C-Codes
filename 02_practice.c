#include<stdio.h>

int main(){
    int radius = 1;
    float pi = 3.14;
    printf("The area of the circle is %f\n", pi*radius*radius);
    int height = 2;
    printf("The volume of the cylinder is %f\n",pi*radius*radius*height);
    return 0;
}